# Notes moved

The biomech pipeline notes now live in their own home base:

    projects/biomech/docs/

Start at `projects/biomech/MEMORY.md` (the entry point / index for the whole
biomech effort), then see `projects/biomech/docs/`.
